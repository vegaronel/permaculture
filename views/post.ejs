<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= post.title %></title>
</head>
<body>
    <h1><%= post.title %></h1>
    <p><%= post.content %></p>
    
    <% if (post.image) { %>
        <img src="<%=post.image %>" alt="Post Image" style="max-width: 100%; height: auto;" />
    <% } %>
    
    <p>Posted by: <%= post.userId.firstname %> <%= post.userId.lastname %></p>
    <p>Created at: <%= post.createdAt.toLocaleString() %></p>

    <h2>Comments</h2>
    <div id="comments">
        <% comments.forEach(comment => { %>
            <div class="comment">
                <p><strong><%= comment.userId.firstname %> <%= comment.userId.lastname %>:</strong> <%= comment.comment %></p>
                <p>Commented at: <%= comment.createdAt.toLocaleString() %></p>
            </div>
        <% }) %>
    </div>
    
    <form action="/comment/<%= post._id %>" method="POST">
        <input type="text" name="comment" required placeholder="Add a comment...">
        <button type="submit">Comment</button>
    </form>
</body>
</html>
